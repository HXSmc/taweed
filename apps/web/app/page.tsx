// Placeholder home page. Taweed's UI (dashboards, ingest, appeals) is built in
// the IMPLEMENT phase (build-plan §8 wk5+). CREATE ships the data pipeline only.
export default function Home() {
  return (
    <main>
      <h1>Taweed</h1>
      <p>Data pipeline is live. UI is built in a later phase.</p>
    </main>
  );
}
