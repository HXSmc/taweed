.claude/
ECC_GUIDE.md

# node / build
node_modules/
dist/
coverage/
*.tsbuildinfo
.turbo/

# next
.next/
apps/web/next-env.d.ts

# env
.env
.env.local

# terraform / secrets (never commit state, vars, or key material)
*.tfstate
*.tfstate.*
*.tfvars
*.pem
*.key

# macOS
.DS_Store

# live-eval JSON report output (packages/*/evals/**/*.eval.ts, AI_EVALS_LIVE=1 only)
packages/*/evals/.output/

# private planning artifacts / local-only working docs (not published)
docs/superpowers/
docs/NEXT_STEP_PROMPT.md
docs/STEP_SUMMARY.md
docs/blocker.md
docs/screenshots/
docs/minimap.md
# temp: injected for live a11y audits, never commit
apps/web/public/axe-core-audit.min.js
# temp: chrome-devtools MCP manual-QA fixtures, never commit
.scratch/
